{{- if .Values.writer.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-writer
  labels:
    app.kubernetes.io/name: gigapipe
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: writer
spec:
  replicas: {{ .Values.writer.replicas }}
  selector:
    matchLabels:
      app.kubernetes.io/name: gigapipe
      app.kubernetes.io/instance: {{ .Release.Name }}
      app.kubernetes.io/component: writer
  template:
    metadata:
      labels:
        app.kubernetes.io/name: gigapipe
        app.kubernetes.io/instance: {{ .Release.Name }}
        app.kubernetes.io/component: writer
    spec:
      initContainers:
        - name: wait-for-clickhouse
          image: busybox:1.36
          command: ['sh', '-c', 'until nc -z {{ .Values.writer.clickhouse.server }} {{ .Values.writer.clickhouse.port }}; do echo "Waiting for ClickHouse..."; sleep 2; done']
      containers:
        - name: gigapipe
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.writer.port }}
          env:
            - name: CLICKHOUSE_SERVER
              value: {{ .Values.writer.clickhouse.server | quote }}
            - name: CLICKHOUSE_PORT
              value: {{ .Values.writer.clickhouse.port | quote }}
            - name: CLICKHOUSE_AUTH
              value: {{ .Values.writer.clickhouse.auth | quote }}
            - name: CLICKHOUSE_DB
              value: {{ .Values.writer.clickhouse.db | quote }}
            - name: MODE
              value: {{ .Values.writer.mode | quote }}
            - name: PORT
              value: {{ .Values.writer.port | quote }}
            - name: BULK_MAX_AGE_MS
              value: "200"
          resources:
            {{- toYaml .Values.writer.resources | nindent 12 }}
          livenessProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10
            failureThreshold: 6
          readinessProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
            failureThreshold: 3
{{- end }}
