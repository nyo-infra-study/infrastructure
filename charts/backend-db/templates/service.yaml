apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
  labels:
    app: {{ .Release.Name }}
spec:
  ports:
  - port: {{ .Values.db.port }}
    targetPort: 5432
    protocol: TCP
  selector:
    app: {{ .Release.Name }}
  type: ClusterIP
