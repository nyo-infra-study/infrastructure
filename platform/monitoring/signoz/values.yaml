# ============================================================
# SigNoz — Reference Values (Local Dev)
# ============================================================
# NOTE: These values are inlined in the ArgoCD Application at
#   apps/dev/monitoring-signoz.yaml (using helm.valuesObject)
#   to ensure proper subchart dependency resolution.
#
# This file is kept as documentation / reference only.
#
# Chart: https://github.com/SigNoz/charts (signoz/signoz v0.121.0)
# Docs:  https://signoz.io/docs/install/argocd/
#
# UI accessible at: http://localhost:9000/signoz
# SigNoz OTel Collector (from within cluster):
#   gRPC: dev-monitoring-signoz-otel-collector.monitoring.svc.cluster.local:4317
#   HTTP: dev-monitoring-signoz-otel-collector.monitoring.svc.cluster.local:4318
# ============================================================
#
# Known issues with chart 0.121.0:
#   - clickhouse.zookeeper.image defaults to signoz/zookeeper:3.7.1
#     but that tag does not exist on Docker Hub. Use 3.7.2 instead.
#   - Multi-source ArgoCD ($values ref) can fail to pass parent chart
#     values into subcharts. Use single-source with valuesObject instead.
# ============================================================
