# ============================================================
# Receivers — App OTLP + ArgoCD Self-Monitoring
# ============================================================
# OTLP receiver for application telemetry (traces, metrics, logs)
# plus Prometheus scrapers for ArgoCD self-metrics.
# ============================================================
config:
  receivers:
    otlp:
      protocols:
        grpc:
          endpoint: 0.0.0.0:4317
        http:
          endpoint: 0.0.0.0:4318

    # ── ClickHouse metrics ──
    prometheus/clickhouse:
      config:
        scrape_configs:
          - job_name: 'clickhouse'
            scrape_interval: 30s
            static_configs:
              - targets: ['dev-monitoring-clickhouse.monitoring.svc.cluster.local:9363']
                labels:
                  namespace: monitoring

    # ── Gigapipe self-metrics ──
    prometheus/gigapipe:
      config:
        scrape_configs:
          - job_name: 'gigapipe-writer'
            scrape_interval: 30s
            static_configs:
              - targets: ['dev-monitoring-gigapipe-writer.monitoring.svc.cluster.local:3100']
                labels:
                  namespace: monitoring
          - job_name: 'gigapipe-reader'
            scrape_interval: 30s
            static_configs:
              - targets: ['dev-monitoring-gigapipe-reader.monitoring.svc.cluster.local:3100']
                labels:
                  namespace: monitoring

    # ── ArgoCD metrics (sync status, app health, reconciliation) ──
    prometheus/argocd:
      config:
        scrape_configs:
          - job_name: 'argocd-server-metrics'
            scrape_interval: 30s
            static_configs:
              - targets: ['argocd-server-metrics.argocd.svc.cluster.local:8083']
                labels:
                  namespace: argocd
          - job_name: 'argocd-repo-server-metrics'
            scrape_interval: 30s
            static_configs:
              - targets: ['argocd-repo-server-metrics.argocd.svc.cluster.local:8084']
                labels:
                  namespace: argocd
          - job_name: 'argocd-metrics'
            scrape_interval: 30s
            static_configs:
              - targets: ['argocd-application-controller-metrics.argocd.svc.cluster.local:8082']
                labels:
                  namespace: argocd
