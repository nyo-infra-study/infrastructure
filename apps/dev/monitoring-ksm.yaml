apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: dev-monitoring-ksm
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "1"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  labels:
    environment: dev
    domain: monitoring
spec:
  project: default
  source:
    chart: kube-state-metrics
    repoURL: https://kubernetes.github.io/kube-state-metrics
    targetRevision: 5.27.0
    helm:
      releaseName: dev-monitoring-ksm
  destination:
    server: https://kubernetes.default.svc
    namespace: monitoring
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
